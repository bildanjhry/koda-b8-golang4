# Program interaktif mencari nama

Program mencari nama dari sebuah slice yang dikirimkan lewat sebuah parameter menggunakan export packages

### Function SearchPerson terdapat pada folder/package libs

```
libs/
  search.go
```

### Aksi interaktif menggunakan package ```fmt.Scanf()```


